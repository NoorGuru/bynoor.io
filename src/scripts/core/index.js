/**
 * core/ — Central orchestration modules
 *
 * Planned modules:
 * - app.js          → Entry point, module initialization
 * - scroll-engine.js → Scroll orchestrator (scene progress, lifecycle events)
 * - scene-manager.js → Scene lifecycle management
 * - nav.js          → Morphing navigation (dots → pill → expanded)
 */
